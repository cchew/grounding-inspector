[build]
  base    = "web"
  command = "npm install && npm run build"
  publish = "dist"

[[edge_functions]]
  path     = "/*"
  function = "gate"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
